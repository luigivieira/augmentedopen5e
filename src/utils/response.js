// TODO: Implement standardized JSON response formatters.
// Keeps the API response structure predictable (status, data, metadata, errors).

export const jsonResponse = (data, status = 200) => {};
export const errorResponse = (message, status = 500) => {};
